{
  "surface": "synthetic",
  "surfaceVersion": "0.0.0",
  "event": "agent.boot",
  "channel": "event",
  "transport": "http",
  "trigger": "process start",
  "payloadSchema": {
    "type": "object",
    "required": ["kind", "sid", "workdir", "label"],
    "properties": {"kind": { "type": "string" }, "sid": { "type": "string" }, "workdir": { "type": "string" }, "label": { "type": "string" }}
  },
  "firstSeen": "0.0.0",
  "confidence": "unverified",
  "notes": "Invented. Field names deliberately differ from claude's so a claude-shaped assumption fails loudly."
}
